**What this changes and why**

**Evidence** — one line per claim you are making about it, with a class:

- [tested] pinned by `<test name>`
- [executed] ran `<command>`, observed `<result>`
- [read] read the code, did not run it
- [inferred] reasoned, not verified

`./tests/run.sh` passes.
